name: shekharh500 Readme

on:
  schedule:
    # Runs at 12am IST
    - cron: '30 18 * * *'
  workflow_dispatch:

jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Use a secret
        run: echo "My secret value is waka_50c9e435-fe60-4628-83c8-07cede83891b"

      - name: Run shekharh500 action
        uses: shekharh500/shekharh500@main
        with:
          WAKATIME_API_KEY: "waka_50c9e435-fe60-4628-83c8-07cede83891b"
